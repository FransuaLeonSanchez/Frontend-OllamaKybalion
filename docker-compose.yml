services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ollama-kybalion-frontend
    restart: always
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    networks:
      - kybalion-network

networks:
  kybalion-network:
    driver: bridge

